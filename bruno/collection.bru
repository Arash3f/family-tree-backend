meta {
  name: Family Tree API
}

auth {
  mode: bearer
}

auth:bearer {
  token: {{access_token}}
}

docs {
  # Family Tree API

  All protected endpoints require `Authorization: Bearer {{access_token}}`.

  ## Flow
  1. Select the Local environment
  2. Run Auth / Login (uses `{{username}}` / `{{password}}`)
  3. Optionally run Auth / Me to verify the token
  4. Use other folders; tokens and created IDs are saved to Local env
  5. For Closest Relationship / Create Marriage, set `from_person_id` and `to_person_id`

  ## Common error body
  ```json
  {
    "error_code": 1301,
    "status": 403,
    "message": "Permission denied"
  }
  ```
  - `401` → Not authenticated
  - `403` → Permission denied
  - `404` → Entity not found
  - `422` → Validation error
}
