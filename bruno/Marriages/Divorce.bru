meta {
  name: Divorce
  type: http
  seq: 6
}

post {
  url: {{base_url}}/family-trees/{{tree_id}}/marriages/divorce
  body: json
  auth: inherit
}

body:json {
  {
    "marriage_id": "{{marriage_id}}",
    "divorced_at": "2024-06-01"
  }
}

assert {
  res.status: eq 200
  res.body.result: isDefined
}

docs {
  # Divorce

  ## Input (`application/json`)
  | Field | Type | Required | Notes |
  |-------|------|----------|-------|
  | marriage_id | uuid | yes | |
  | divorced_at | date | yes | `YYYY-MM-DD` |

  ## Output `200`
  ```json
  { "result": "ok" }
  ```
}
