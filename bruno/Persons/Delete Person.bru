meta {
  name: Delete Person
  type: http
  seq: 4
}

delete {
  url: {{base_url}}/persons/{{person_id}}
  body: none
  auth: inherit
}

params:path {
  person_id: {{person_id}}
}

assert {
  res.status: eq 200
  res.body.result: isDefined
}

docs {
  # Delete Person

  ## Input (path)
  | Param | Type | Required |
  |-------|------|----------|
  | person_id | uuid | yes |

  ## Output `200`
  ```json
  { "result": "ok" }
  ```
}
