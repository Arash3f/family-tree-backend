# Local-only overrides to avoid host port conflicts during CI simulation.
services:
  db:
    ports:
      - "15432:5432"
  redis:
    ports:
      - "16379:6379"
  neo4j:
    ports:
      - "17474:7474"
      - "17687:7687"
